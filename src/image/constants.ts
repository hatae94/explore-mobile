/**
 * 이미지 처리 상수 (SPEC-IMAGE-001 §D.1).
 *
 * 숫자를 코드 여러 곳에 흩뿌리지 않는다 — 흩뿌리면 한 곳만 고쳤을 때
 * 나머지가 조용히 어긋난다. 이 파일이 그 숫자들의 유일한 집이다.
 *
 * **아래 값은 잠정값이다.** plan.md §A.7이 M5(실기기 검증)가 확정한다고
 * 적었다. 잠정값의 근거는 plan-phase 실측(progress.md §B)이며, 그 실측은
 * 파일 크기만 잰 것이지 **판독 가능성을 재지 않았다** — 축소한 이미지에서
 * 목표 요소의 라벨이 실제로 읽히는지는 AC-IMAGE-016이 3종 화면에서 닫는다.
 */

/**
 * 축소 시 긴 변의 상한(px). 이 값보다 긴 변만 줄어들며, 짧으면 그대로 둔다
 * (REQ-IMAGE-001 — 확대는 하지 않는다).
 *
 * **M5(AC-IMAGE-016)가 이 값을 확정했다** (2026-08-10, SM_G960N + iPad Pro 12.9):
 * 밀도가 다른 3종 화면(홈 / 설정 목록 / 기기정보 밀집 화면)을 1568px과
 * 1024px 두 상한으로 찍어 실제로 판독했다. 밀집 화면의 가장 작은 문자열
 * (일련번호·IMEI 계열)이 **1024px에서도 전부 읽혔고** 파일은 절반이었다
 * (103KB → 55KB).
 *
 * @MX:NOTE — 이 값의 근거에는 알려진 구멍이 있다: 1024px 판독 확인은
 * **Android 밀집 화면 1종 관측**이며, 표·채팅 타임라인·작은 글씨 웹페이지는
 * 재보지 않았다. 그런 화면에서 판독이 깨지면 값을 바꾸는 것이 아니라
 * 호출자가 `--max-edge 1568`로 그 회차만 올리면 된다 — 기본값을 올리기 전에
 * 어느 화면에서 깨졌는지부터 기록할 것.
 */
export const DEFAULT_MAX_EDGE = 1024;

/** 축소 시 재인코딩 포맷 (REQ-IMAGE-001). PNG 대비 파일 크기가 압도적으로 작다. */
export const DEFAULT_FORMAT = "jpeg" as const;

/**
 * JPEG 품질(0~100). 잠정값이며 M5가 확정한다.
 *
 * @MX:NOTE — q75는 plan-phase 실측에 쓰인 값 그대로다. 이 값에서 iPad 홈
 * 화면 1장의 작은 글씨가 판독됐다는 관측이 있으나 **1장 관측이며
 * 일반화하지 않는다**(spec.md §C.2).
 */
export const DEFAULT_QUALITY = 75;

/**
 * `--from`이 참조하는 캡처의 신선도 상한(ms). 이 시간을 넘긴 캡처로는
 * 좌표 변환을 거부한다 (REQ-IMAGE-006).
 *
 * **M5 실측이 이 값을 60초에서 300초로 올렸다** (2026-08-10, SM_G960N):
 * 잠정값 60초로 실제 비전 루프(캡처 → 이미지 판독 → 좌표 결정 → 탭)를 돌리자
 * **62초 경과로 거부**됐다 — 정상적인 한 바퀴가 상한을 넘겼다. 60초는 진짜
 * 낡은 캡처가 아니라 **정상 사용을 막는 값**이었다.
 *
 * @MX:NOTE — 300초는 위 실측(정상 루프 1회 62초)에 여유를 둔 값이지, 화면
 * 전환 속도를 잰 값이 아니다. 이 검사가 막으려는 것은 "긴 중단 뒤 재개하며
 * 몇 분 전 캡처로 탭하는 사고"이며, 에이전트가 자기 조작으로 화면을 바꾼
 * 직후의 좌표 오용은 초 단위여서 어떤 상한으로도 잡히지 않는다 — 이 상수의
 * 한계를 값보다 먼저 알아야 한다.
 */
export const CAPTURE_STALE_MS = 300_000;
