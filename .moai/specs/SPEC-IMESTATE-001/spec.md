---
id: SPEC-IMESTATE-001
title: "기기별 IME 세션 상태 격리 — 병렬 텍스트 입력에서 복원 기록이 유실되지 않는다"
version: "0.4.0"
status: in-progress
created: 2026-07-29
updated: 2026-07-30
author: hatae
priority: P1
phase: "v0.4.0 target"
module: "src/backend/"
lifecycle: spec-anchored
tags: "ime, session-state, multi-device, concurrency, reset, adbkeyboard, android"
tier: M
depends_on: [SPEC-ANDROID-001]
---

# SPEC-IMESTATE-001 — 기기별 IME 세션 상태 격리

## HISTORY

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| 0.1.0 | 2026-07-29 | hatae | 최초 작성. 로드맵의 "SPEC-04 탐색 루프" 자리표시자를 실제 점검으로 대체한 결과물 — 점검 결론은 "두 기기 시나리오에 필요한 원시 동작은 이미 충분하다"였고, 유일하게 나온 갭 1건을 이 SPEC이 닫는다. |
| 0.2.0 | 2026-07-29 | hatae | plan-auditor 1차 감사 **FAIL(0.63 / Tier M 기준 0.80)** 반영. MUST-FIX 10건 + SHOULD-FIX 11건. 판정을 끌어내린 것은 Testability 0.50 — **결함이 있어도 무조건 PASS하는 공허한 검사 2건**(AC-008의 grep 패턴이 실제로 매치되지 않음, AC-018의 `git diff`가 커밋 후 항상 빈 출력)이 REQ 보호의 유일한 기계적 증거였다. 실질 변경 4건: ① **동일 시리얼 경합을 범위에 편입** — 기기별 파일 분리는 `adb-backend.ts:454-457`의 check-then-act를 전혀 보호하지 않으며 증상이 이 SPEC의 결함과 동일하다(REQ-007 신설) ② **이관 창 좀비를 툼스톤으로 닫는다** — 0.1.0 §C.2의 "닫으려면 잠금이 필요하다"는 미검증 주장이었고 잠금 없는 해법이 존재한다(REQ-008 신설, "잔재"의 조작적 정의 확정) ③ **무방어 공유 자원은 하나가 아니라 둘이었다** — `src/webview/calibration.ts`의 `CalibrationStore`가 구조적으로 동일한 결함이며 §C.4로 이월(피해 등급 차이가 이월 근거, 미공개는 결함) ④ 공개 API 자백을 export 4종으로 확장. 그리고 **전제가 재현됐다** — 0.1.0 §C.1-⑪의 "재현하지 않았다"는 낡았다(§C.1-⑪ 관측 / ⑰ 미관측으로 분리). |
| 0.3.0 | 2026-07-30 | hatae | plan-auditor **2차 감사 FAIL(0.77 / 기준 0.80, 1차 0.63 → +0.14, 회귀 없음)** 반영. 1차 지적 21건 중 20건 CLOSED로 확인됐고(공허 검사 2건은 감사관이 명령을 재실행해 동작 변화 확인), **신규 결함 3건이 전부 0.2.0이 추가한 표면(REQ-007·008)에서 나왔다** — 범위를 넓히며 새 구멍을 냈다. ① **REQ-005 ↔ REQ-008 모순**: "레코드가 없었다면 조회 결과를 바꾸지 않는다"가 무조건형이라, 구 파일 기록만 있는 시리얼의 `clear`가 툼스톤을 만들지 않아도 되는 것으로 읽힌다 → REQ-008이 닫았다고 선언한 이관 창 좀비가 그대로 열리고, **그것을 잡는 AC가 하나도 없었다**(AC-023은 툼스톤을 Given으로 미리 만들어 두고 조회만 검증). ② **이관이 툼스톤을 우회**: 조회 1단계(레코드)가 2단계(툼스톤)보다 앞서므로, 이관이 툼스톤 있는 시리얼의 레코드를 만들면 억제가 무력화된다(감사관이 명세대로 시뮬레이션해 재현). 게이트 요구도 **이관 단위 규정도 없었다** — §D가 스스로 금지한 "정확성 기전을 구현 재량으로 미루는" 패턴이 이 SPEC 안에서 일어났다. ③ **REQ-007의 실패 처리가 무자격**: "실패는 오류가 아니라 정상 결과"가 종류를 한정하지 않아 ENOENT·EACCES·ENOSPC를 삼키면 기록 없이 정상 종료 → **이 SPEC이 없애려는 결함과 동일한 조용한 실패**를 새로 만든다. 함께 처리: `clear` 연산 순서를 **툼스톤 우선**으로 확정(`plan.md`가 위험한 unlink→create 순서를 명시하고 있었다), 이관 단위를 **일괄 + 툼스톤 게이트**로 확정, §C.2 마일스톤 오기 2건 정정, §C.1-⑫ 관측/미관측 분할 보강, `plan.md` §E의 양성 대조 없는 잠금 grep 제거(SPEC 자신의 새 원칙 위반이었다). REQ 8 유지 / AC 24 → **27**. |
| 0.4.0 | 2026-07-30 | hatae | plan-auditor **3차(최종) 감사 FAIL(0.76 / 기준 0.80)** 반영. 2차 0.77 대비 **하락**이므로 규칙상 STOP 신호가 발동했고, 감사 반복은 **3/3 소진**됐다(4차 없음). 신규 MUST-FIX 4건 중 2건이 다시 **0.3.0이 추가한 표면**에서 나왔다 — 확대가 새 결함을 낳는 패턴이 3라운드 연속이다. 사용자 결정으로 **범위를 축소**한다. ① **이관·폐기 기능을 제외한다** — 구 단일 파일은 이관·rename·폐기 없이 **영구 읽기 전용 폴백**으로 남는다(REQ-003 전면 개정, §A.3-⑧의 이관 단위·툼스톤 게이트 삭제, AC-011·013·026 폐기, AC-028 신설). 근거: 이관이 제공하려던 가치는 **이미 다른 장치로 달성돼 있다** — 살아있는 시리얼은 조회 1단계(레코드)가 3단계(구 파일)보다 앞서 자동으로 가려지고, 무효화된 시리얼은 툼스톤이 3단계를 억제한다. 즉 "이관해야 구 파일이 조회에서 빠진다"는 전제가 성립하지 않았고, 이관은 **가치는 거의 없고 위험은 최대**인 부분이었다(`plan.md` §B.1이 스스로 "최고 리스크 — 설계 미확정"으로 표시한 지점이며 3차 MUST-FIX 2건의 출처). 아무것도 이동·삭제하지 않으므로 유실 경로가 원천 소멸한다. 대가는 구 파일이 영구히 남는 것 하나이며, 이는 §C.2가 툼스톤에 대해 이미 수용한 것과 같은 종류의 거래다. ② **§A.3-⑧을 새 세션 경로의 순서 확정으로 교체** — 레코드 배타 생성 → 툼스톤 제거 순서를 못박는다(N3-MF-1: ⑦이 `clear` 쪽 순서만 정하고 형제 경로를 비워 두어, 툼스톤을 먼저 지우면 그 창의 조회가 3단계로 떨어졌다). ③ **REQ-007의 비-`EEXIST` 처리를 "거부한다"로 관찰 가능하게 확정**(N3-MF-3: AC-027의 "구별되어 처리된다"가 관찰 대상을 명시하지 않아 판정 불가였다 — 1차 감사가 지적한 공허 검사의 재발이다). ④ REQ-002의 시리얼 길이 상한 **127 → 123바이트 정정**(N3-SF-4: `127×2+8(.cleared)=262 > 255`, 실측 `123×2+8=254`). 그리고 기준 SHA 보관 위치를 `plan.md` §C에서 **`progress.md` §E.2로 이전**(N3-MF-4: `manager-develop`은 `plan.md` 본문 수정이 금지되어 REQ-006의 유일한 기계적 증거가 실행 불가였다). REQ 8 유지 / AC **25 활성 + 3 폐기**. 이 개정은 감사 반복 소진으로 **감사받지 못했다** — 대체 검증은 삭제 참조 전수 grep이며 §C.1-㉒에 기록한다. |

---

## §A. 개요 (Context & Goal)

### A.1 배경 — 점검이 남긴 갭

로드맵(`.moai/project/product.md:101`)의 "SPEC-04 — 탐색 루프 + 멀티 기기 시나리오 오케스트레이션"은 **신규 기능 항목이 아니었다.** `interview.md:39`가 탐색 루프·재시도 전략·화면 판단을 **OUT**(에이전트 담당)으로 못박았고, `interview.md:43`이 그 파급을 기록해 두었다:

> SPEC-04는 신규 기능 SPEC이 아니라 **"두 기기 상호작용에 필요한 원시 동작이 이미 충분한가"를 점검하는 성격**이 된다.

그 점검을 2026-07-29에 실행했다. 두 기기 시나리오("기기 A를 조작 → 기기 B에서 결과 확인")가 요구하는 능력을 현행 코드와 대조한 결과, **10가지가 이미 충족되어 있었다**:

| 필요 능력 | 충족 근거 |
|---|---|
| 두 기기 동시 발견·구분 | `devices`가 adb+idb 목록을 병합. `DeviceInfo`(`src/schema/device-backend.ts:90-103`)가 `serial`·`model`·`osVersion`·`platform`·`isEmulator`·`connectionState` 제공 |
| 명령별 기기 지정 | `--device <serial>` — `devices`를 제외한 **11개 명령 전부** `resolveTargetDevice` 사용 |
| 엉뚱한 기기 조작 방지 | 2대 이상 연결 + `--device` 생략 → `AMBIGUOUS_DEVICE`, 첫 기기를 임의 선택하지 않음(`device-targeting.ts:92-103`) |
| 크로스플랫폼 라우팅 | `BackendRegistry` 파사드가 시리얼→소유 백엔드로 위임 |
| 화면 읽기 | `dump`(공통 `CommonElement`), `screenshot` |
| 조작 | `tap` `text` `key` `swipe` `scroll` |
| 앱 제어 | `launch` `stop` |
| 동시 `dump` 파일 충돌 | 기기측 임시 경로에 시리얼 + **랜덤 접미사**(`adb-backend.ts:49-59`, REQ-MULTIDEV-004) |
| 스크린샷 파일 충돌 | 기기측 임시 파일 자체가 없음 — `adb exec-out` 스트리밍, 호스트 경로는 `--out`으로 호출자 지정 |
| 한글 입력 첫 호출 삼킴 | `waitForImeBindingReady`가 IME 전환 **후** · 입력 **전**에 대기(`adb-backend.ts:388, 464, 647`) |

이 중 **동시 실행을 명시적으로 겨냥해 설계된 것은 `dump`의 랜덤 접미사 하나뿐**이다(`adb-backend.ts:49-54`가 "even concurrent `dump` invocations targeting the SAME serial from separate CLI processes never race"라고 진술 — **동일** 시리얼까지 방어 대상으로 삼는다). 스크린샷의 무충돌은 스트리밍 설계의 부수 결과이고(`screenshot.ts:4-8`은 동시성을 언급하지 않는다), IME 바인딩 대기는 **단일 기기 콜드 경로의 타이밍 결함** 대응이다(`adb-backend.ts:386-397`). 0.1.0은 이 세 항목을 모두 "동시 실행을 겨냥한 설계"로 귀속했으나 3중 2가 사실과 달랐다.

**무방어 공유 자원은 둘 남았다** — IME 세션 저장소와 웹 캘리브레이션 캐시다. 이 SPEC은 그중 **유실 비용이 조용한 복원 불가로 직결되는 IME 세션 저장소**를 닫고, 다른 하나는 §C.4로 이월한다. `src/backend/ime-session-store.ts:22-27`의 `@MX:NOTE`가 전자를 직접 서술한다:

> read-modify-write is NOT atomic across concurrent writers: two `AdbBackend` processes racing to establish sessions for two **DIFFERENT serials** at the same instant could clobber each other's write (last write wins, dropping the other's entry). This is an accepted, pre-existing limitation (best-effort persistence, not a distributed lock) — the common case, one CLI invocation at a time, is unaffected.

즉 **이 결함은 새로 발견된 것이 아니라, 코드가 스스로 기록해 둔 채 "일반적인 경우에는 무영향"이라는 전제 아래 수용된 것**이다. 이 SPEC이 여는 근거는 결함의 발견이 아니라 **그 전제의 소멸**이다: 두 기기 시나리오는 정의상 "한 번에 한 CLI 호출"이 아니다.

#### 결함 ① 시리얼 간 유실 (0.1.0이 식별)

비-ASCII(한글·이모지) 텍스트를 입력하려면 `AdbBackend`가 기기의 기본 IME를 ADBKeyBoard로 전환하고, 나중에 `reset`이 복원할 수 있도록 "전환 직전의 IME"를 기록해 둔다. 매 CLI 호출이 **별개의 OS 프로세스**이므로(`ime-session-store.ts:5-11`) 이 기록은 디스크에 남아야 하고, 현재는 **모든 기기가 공유하는 단일 파일** `<cache-dir>/ime-sessions.json`에 `{ [serial]: { originalIme } }` 형태로 저장된다.

두 기기에 병렬로 비-ASCII 텍스트를 입력하면, `setOriginalIme` **내부의** read-modify-write가 겹친다:

```
프로세스 A (기기 A)                프로세스 B (기기 B)
setOriginalIme 진입                setOriginalIme 진입
  readAll() → {}                     readAll() → {}
  sessions[A] = {...}                sessions[B] = {...}
  writeAll({A})  ────────┐           writeAll({B})  ────────┐
                         └───────────────────────────────────> 나중 쓰기가 이김
디스크 최종: { B: {...} }           ← A의 기록이 사라졌다
```

#### 결함 ② 동일 시리얼 경합 (0.2.0이 편입 — 감사 지적)

기기별 파일 분리는 결함 ①만 없앤다. **같은 기기에 `text`를 동시에 두 번** 호출하면 그 기기의 파일은 여전히 하나이고, `adb-backend.ts:454-457`이 check-then-act이다:

```ts
const existingOriginal = await this.imeSessions.getOriginalIme(serial);
if (existingOriginal === undefined) {
  await this.imeSessions.setOriginalIme(serial, currentIme);
}
```

두 프로세스가 모두 `undefined`를 읽고 모두 쓴다. 그런데 바로 위 주석(`:446-452`)은 이 가드의 목적을 명시한다 — *"recording ADBKeyBoard itself as 'the original' is the exact cross-process bug this fixes"*. **그 가드가 정확히 이 경합에 무너진다**: 늦게 진입한 프로세스가 이미 ADBKeyBoard로 전환된 기기의 IME를 "원래 IME"로 기록하고, `reset`은 기기를 **ADBKeyBoard 자체로 복원**한다.

사용자에게 보이는 결과가 결함 ①과 **동일**(복원 불가)하므로 같은 SPEC에서 닫는다. 그리고 `deviceDumpPath`가 **동일 시리얼 동시 호출을 실재하는 시나리오로 간주해 방어**하는 프로젝트에서, IME 경로만 그것을 무시할 근거가 없다.

#### 사용자에게 보이는 결과 (양쪽 공통)

**기록이 유실되거나 잘못 기록된 기기는 `reset`을 실행해도 원래 IME로 복원되지 않는다.** 실기기에는 ADBKeyBoard가 활성 키보드로 남아 사람이 직접 손으로 되돌려야 한다. 오류도 나지 않는다 — 조용한 실패다.

### A.2 목표 (WHY)

두 기기 시나리오에서 각 기기의 IME 복원 기록이 **다른 기기의 동시 작업 때문에도, 같은 기기의 동시 작업 때문에도** 유실되거나 오염되지 않게 한다. 목표는 레이스가 발생했을 때 잘 처리하는 것이 아니라, **레이스가 성립할 수 없게 만드는 것**이다 — 시리얼 간에는 공유 자원을 제거해서(REQ-001), 동일 시리얼에는 조건부 쓰기를 단일 원자 연산으로 만들어서(REQ-007).

부차 목표: 이 변경이 기존 단일 기기 동작을 바꾸지 않는다. 프로덕션 호출자(`AdbBackend`)와 간접 호출자(`reset.ts` · `doctor.ts`)는 수정하지 않는다.

### A.3 확정된 설계 결정 (USER-APPROVED — FIXED)

2026-07-29 사용자 승인(①~④는 초기 승인, ⑤~⑥은 감사 반영 승인). 구현 단계에서 재논의 대상이 아니다.

**① 기기별 파일 분리로 공유 자원을 제거한다**

단일 `ime-sessions.json`을 버리고, 시리얼마다 전용 레코드 파일을 쓴다:

```
<cache-dir>/ime-sessions/<encoded-serial>.json
```

서로 다른 시리얼은 서로 다른 파일을 쓰므로 read-modify-write가 겹칠 대상 자체가 없다. **레이스를 감지·직렬화하는 것이 아니라 성립 불가능하게 만드는 선택**이다.

구 파일 `<cache-dir>/ime-sessions.json`(파일)과 신규 `<cache-dir>/ime-sessions/`(디렉터리)는 이름이 달라 대소문자 무구분 파일시스템에서도 공존한다. 0.4.0에서 이관을 제외했으므로 둘은 **영구히** 공존한다 — 구 파일은 읽기 전용 폴백으로 남고 신규 디렉터리가 모든 쓰기를 받는다(REQ-003).

**② 파일 락(lock)을 도입하지 않는다**

단일 파일을 유지하고 배타 잠금으로 직렬화하는 대안은 기각한다. 이유: 잠금 자체가 상태가 되어 ① 프로세스가 죽어 남긴 부생 잠금(stale lock)의 통과 기준 ② 대기 상한 ③ 대기 초과 시 산출 계약 — 셋을 모두 새로 정의해야 하고, 그 중 ③은 **"CLI는 절대 멈추지 않는다"는 기존 계약과 정면으로 부딪히는 새 실패 경로**를 만든다. ⑤·⑥이 잠금 없이 남은 두 경합을 닫으므로, 이 기각은 사각지대를 남기지 않는다.

**③ 원자적 쓰기(임시 파일 → rename)만으로는 결함 ①이 고쳐지지 않는다 — 오답으로 명시 기각**

`rename(2)`의 원자성은 "파일이 반쯤 쓰인 상태로 관측되지 않음"만 보장한다. 두 프로세스가 각자 `{}`를 읽고 각자 자기 항목만 담은 **전체 맵**을 쓰는 구조는 그대로이므로, 나중 쓰기가 여전히 앞선 기록을 덮는다. 이 오답은 흔하므로 SPEC 층위에 기각 근거를 남긴다.

**④ 파일명 인코딩은 `adb-backend.ts:57`의 것을 재사용하지 않는다**

```ts
const safeSerial = serial.replace(/[^A-Za-z0-9_-]/g, "_");   // 재사용 금지
```

이 변환은 **손실이 있다**: `192.168.1.5:5555` → `192_168_1_5_5555`. `dump`의 기기측 경로에서는 무해하다(기기마다 파일 시스템이 독립이고 랜덤 접미사가 붙는다). 그러나 IME 기록을 기기별 파일로 쪼개는 데 쓰면 **서로 다른 두 시리얼이 같은 파일명으로 매핑될 수 있고, 그 순간 고치려던 결함이 그대로 재현된다** — 위치만 단일 파일에서 파일명 충돌로 옮긴 셈이다. 두 시리얼이 실제로 같은 값으로 붕괴함을 실행으로 확인했다(§C.1-⑨). REQ-002가 대체 규칙을 정의한다.

**⑤ 동일 시리얼의 "부재식 기록"은 단일 원자 연산으로 만든다 (잠금 없음)**

레코드 파일 생성을 **배타 생성**(`O_CREAT|O_EXCL` 계열 — Node의 `wx` 플래그)으로 수행한다. 파일이 이미 있으면 생성이 실패하고, 그때는 **먼저 기록한 쪽의 값이 이긴다**(덮어쓰지 않는다). 이것이 `adb-backend.ts:454-457`의 "부재일 때만 쓴다"를 원자적으로 만든다.

read-modify-write가 아니라 **단일 시스템 콜의 원자성**에 의존하므로 잠금이 필요 없고 ②와 충돌하지 않는다. 대기·타임아웃·부생 상태가 없으므로 "CLI는 멈추지 않는다" 계약도 유지된다.

**⑥ `clear`는 파일을 지우지 않고 툼스톤(음성 기록)을 남긴다**

`clearOriginalIme(serial)`은 그 시리얼의 **툼스톤 마커**를 만든다. 조회 규칙은 3단계다:

```
1. <enc>.json 이 있으면          → 그 값
2. 없고 <enc>.cleared 가 있으면  → undefined  (구 파일 폴백 억제)
3. 둘 다 없으면                  → 구 파일 폴백 (읽기 전용)
```

툼스톤이 구 파일 폴백을 억제하므로 **구 파일 좀비가 닫힌다** — `clear`된 시리얼의 기록이 구 파일에 남아 있어도 이후 조회가 그것을 되살리지 못한다.

0.4.0에서 이관을 제외해 구 파일이 **영구히** 남으므로, 이 억제는 한시적 창을 막는 장치가 아니라 **영구적으로 유일한 방어**다 — 툼스톤이 없으면 무효화된 기록이 매 조회마다 되살아난다. 툼스톤의 역할이 0.2.0·0.3.0보다 무거워진 것이 범위 축소의 유일한 파급이며, 이 방어는 이미 AC-023·025가 검증한다.

툼스톤은 레코드 파일과 **다른 경로**이므로 ⑤의 배타 생성과 간섭하지 않는다. 새 세션 시작 시의 두 연산(레코드 배타 생성 / 툼스톤 제거)의 **순서는 ⑧이 확정한다.**

0.1.0 §C.2는 이 부활 경로를 "닫으려면 §A.3-②가 기각한 잠금이 다시 필요해진다"며 허용된 한계로 두었다. **그 주장은 미검증이었고 틀렸다** — 툼스톤의 쓰기 주체는 해당 시리얼 하나뿐이므로 잠금 없이 닫힌다. 이 SPEC의 존재 근거가 "이전에 수용된 한계가 사실은 본상이었다"인데, 닫을 수 있는 한계를 다시 수용하는 것은 같은 실패를 반복하는 일이다.

**⑦ `clear`는 툼스톤을 먼저 만들고 그다음 레코드를 제거한다 (순서 확정)**

`clearOriginalIme(serial)`의 두 연산은 **이 순서로** 수행한다:

```
1. <enc>.cleared 생성   (툼스톤 먼저)
2. <enc>.json 제거      (레코드 나중)
```

반대 순서(레코드 제거 → 툼스톤 생성)는 그 사이 창에서 조회가 레코드도 툼스톤도 보지 못해 **3단계 구 파일 폴백으로 떨어진다** — REQ-008이 닫았다고 선언한 좀비가 그 창에서 되살아난다. 툼스톤 우선 순서면 창 중 조회가 1단계 또는 2단계에서 걸려 3단계에 **절대 도달하지 않는다**. `clear`가 두 연산 사이에서 죽어도 툼스톤이 이미 있으므로 억제가 유지된다.

0.2.0은 조회 순서만 규정하고 이 **연산 순서를 규정하지 않았다.** 그 공백 상태에서 `plan.md`가 위험한 반대 순서를 명시하고 있었다 — 정확성에 직결되는 순서이므로 SPEC 층위에서 확정한다(§D 구현 세부 예외와 같은 근거).

**⑧ 새 세션 시작은 레코드를 먼저 만들고 그다음 툼스톤을 제거한다 (순서 확정 — 0.4.0)**

`setOriginalIme(serial, ime)`가 툼스톤이 있는 시리얼에 새 세션을 시작할 때, 두 연산은 **이 순서로** 수행한다:

```
1. <enc>.json 배타 생성   (레코드 먼저)
2. <enc>.cleared 제거     (툼스톤 나중)
```

반대 순서(툼스톤 제거 → 레코드 생성)는 그 사이 창에서 조회가 레코드도 툼스톤도 보지 못해 **3단계 구 파일 폴백으로 떨어진다** — 구 파일에 그 시리얼의 낡은 기록이 남아 있으면 무효화된 값이 되살아나고 `reset`이 그 낡은 IME로 "복원"한다. 레코드 우선 순서면 창 중 조회가 1단계 또는 2단계에서 걸려 3단계에 **절대 도달하지 않는다**. 두 연산 사이에서 죽어도 레코드가 이미 있으므로 조회는 정확하다 — 남은 툼스톤은 1단계가 이기므로 무해하고, 다음 시작이 멱등하게 제거한다.

⑦과 ⑧은 **같은 규칙의 두 방향**이다: 상태를 바꾸는 두 경로 모두 "조회가 3단계로 떨어지는 창을 만들지 않는다"를 지킨다. ⑦은 `clear`(툼스톤 먼저), ⑧은 `set`(레코드 먼저) — 어느 쪽이든 **억제하거나 값을 주는 파일이 항상 하나는 존재한다.**

0.3.0은 ⑦로 `clear` 쪽 순서만 정하고 이 형제 경로를 비워 두었다(3차 감사 N3-MF-1). §D의 `### Out of Scope — 구현 세부(HOW)`가 그 위험을 직접 서술해 두었는데 — *"정확성에 직결되는 기전을 구현 재량으로 미루면 요구사항이 금지한 선택지가 되살아난다"* — 이 SPEC 안에서 같은 일이 두 번 일어났다. ⑦과 ⑧이 두 방향 모두 닫는다.

> 0.3.0의 ⑧(이관 단위 일괄 + 툼스톤 게이트)은 **삭제됐다** — 0.4.0이 이관 기능 자체를 제외했으므로 규정할 대상이 없다. 이관이 툼스톤을 우회하던 위험(2차 감사 N-MF-2)은 기능 제거로 소멸했고, 이관 순서가 `plan.md` §B.1과 모순되던 위험(3차 감사 N3-MF-2)도 함께 소멸했다.

### A.4 아키텍처 — 저장소 계층 1개 교체

```
변경 없음:  AdbBackend                          (직접 호출자 — REQ-006)
변경 없음:  reset.ts · doctor.ts                (간접 호출자 — AdbBackend 메서드 경유)
변경 없음:  DeviceBackend 인터페이스 10개 메서드  (PRESERVE)
   ↓
교체:      ImeSessionStore                      (내부 저장 배치 + 쓰기 원자성)
   ↓
교체:      <cache-dir>/ime-sessions.json         (단일 파일)
        →  <cache-dir>/ime-sessions/<enc>.json    (기기별 레코드, 배타 생성)
        +  <cache-dir>/ime-sessions/<enc>.cleared (기기별 툼스톤)
```

`ImeSessionStore`의 공개 메서드 3개(`getOriginalIme` · `setOriginalIme` · `clearOriginalIme`)는 시그니처가 바뀌지 않는다.

---

## §B. 요구사항 (GEARS)

> 패턴 라벨: **Ubiquitous**(항상) · **Event-driven / When**(사건) · **State-driven / While**(상태 지속) · **Optional / Where**(조건부 기능) · **Unwanted / If-Then**(원치 않는 상황).
> REQ-004의 `If/Then` 표기는 GEARS 정규 부정형(`shall not`) 대비 레거시 표기이며 백워드 호환 창(2026-11-22) 내에서 유효하다.

### B.1 기기별 격리 (REQ-IMESTATE)

**REQ-IMESTATE-001 — 시리얼 간 공유 자원 제거** *(Ubiquitous)*

시스템은 각 기기의 IME 세션 기록을 그 시리얼 전용 파일에 저장한다. **서로 다른 두 시리얼**의 기록은 어떤 순간에도 같은 파일을 대상으로 read-modify-write 하지 않는다.

- 검증 가능 형태: 서로 다른 시리얼 2개에 대해 `setOriginalIme`을 수행한 뒤, 두 기록이 각각 다른 경로에 존재하고 **양쪽 모두 조회된다.**
- 계보: 이 요구는 REQ-MULTIDEV-003(`adb-backend.ts:246, 445`)과 REQ-INPUT-004(`ime-session-store.ts:2`)가 도입한 크로스프로세스 세션 추적을 강화한다.
- 동일 시리얼 경합은 이 요구의 범위가 아니다 — REQ-007이 담당한다.

**REQ-IMESTATE-002 — 파일명 인코딩은 단사이며 대소문자 무구분 파일시스템에서도 안전하다** *(Ubiquitous)*

시리얼 → 파일명 변환은 세 성질을 모두 만족한다:

1. **단사(injective)**: 서로 다른 시리얼은 항상 서로 다른 파일명으로 매핑된다. 충돌은 없다.
2. **대소문자 무구분 안전**: 파일명 대소문자를 구분하지 않는 파일시스템에서도 성질 1이 성립한다 — 변환 결과는 **단일 대소문자 계열**만 사용한다.
3. **파일시스템 안전**: 경로 구분자(`/` `\`), 콜론(`:`), 그 밖의 예약 문자를 남기지 않으며, 캐시 디렉터리 하위를 벗어나는 경로를 생성하지 않는다.

확정 규칙: **파일명 = 시리얼의 UTF-8 바이트를 소문자 16진수로 인코딩한 문자열**, 확장자는 레코드 `.json` / 툼스톤 `.cleared`. 16진수는 단사이고(역변환 가능), 소문자로 고정하므로 성질 2를 만족하며, `[0-9a-f]`만 쓰므로 성질 3을 만족한다.

- 길이: hex는 입력의 2배이고 **확장자가 더 붙으므로**, 파일명 255바이트 한도에서 시리얼은 **123바이트까지** 안전하다 — 가장 긴 확장자 `.cleared`(8자) 기준으로 `123×2+8 = 254 ≤ 255`이고 `124×2+8 = 256`은 초과한다. 0.3.0의 "127바이트"는 확장자를 빼먹은 계산이었다(`127×2+8 = 262 > 255`, 3차 감사 N3-SF-4). 실측된 adb 시리얼·iOS UDID는 모두 40바이트 미만이므로 여유가 크다.
- 가독성 손실 보상: 레코드 파일 **내용**에 원본 시리얼을 별도 필드로 기록한다(추적성 목적. 조회 키는 어디까지나 파일명이다).

**REQ-IMESTATE-003 — 구 단일 파일은 영구 읽기 전용 폴백이다** *(State-driven / While)*

**While** 구 단일 파일 `<cache-dir>/ime-sessions.json`이 존재하고 그 시리얼의 레코드 파일도 툼스톤도 없는 동안, 시스템은 조회 시 구 파일의 기록을 반환한다. 그리고 시스템은 구 파일을 **이관·rename·폐기하지 않으며, 어떤 경로로도 수정하지 않는다.**

- 왜 필요한가: 이 변경이 적용되는 시점에 ADBKeyBoard가 활성 상태인 기기가 있을 수 있고, 그 복원 기록은 구 파일에만 있다. 구 파일을 무시하면 그 기기는 **영구적으로 복원 불가**가 된다 — 결함을 고치면서 새 결함을 만드는 셈이다.
- 우선순위는 REQ-008의 3단계 조회 규칙이 정한다(레코드 > 툼스톤 > 구 파일).
- 쓰기는 항상 신규 경로로만 한다. **구 파일에는 절대 쓰지 않는다** — 구 파일에 항목별 삭제를 수행하는 설계는 공유 파일 read-modify-write이므로 REQ-001 위반이며 명시적으로 금지된다.
- **구 파일은 읽기만 한다** — 쓰기·삭제·rename·이관이 모두 없다. 0.2.0이 항목별 삭제를 REQ-001 위반으로 기각했고, 남은 두 후보(통째 rename / 표식 파일)는 0.4.0이 **기능 자체를 제외**했다.
- 구 파일의 항목이 조회에서 빠지는 방법은 **이관이 아니라 가려짐**이다. 살아있는 시리얼은 레코드 파일(1단계)이 구 파일(3단계)보다 앞서 자동으로 가려지고, 무효화된 시리얼은 툼스톤(2단계)이 폴백을 억제한다. 두 경우가 전부이므로 **이관 없이도 이 REQ의 목적이 달성된다** — 0.3.0까지의 "이관해야 구 파일이 조회에서 빠진다"는 전제는 성립하지 않았다.
- 대가는 구 파일이 영구히 남는 것 하나다(캐시 디렉터리의 작은 JSON 1개). 정리 경로를 두지 않는 것은 **의도된 선택**이며, §C.2가 툼스톤 누적에 대해 이미 수용한 것과 같은 종류의 거래다. 아무것도 이동·삭제하지 않으므로 유실 경로가 존재하지 않는다.
- AC-028이 "읽기만 한다"를 기계적으로 검증한다(폴백 조회 전후로 내용·mtime 불변).

> **0.3.0까지 REQ-003이 요구했던 이관·폐기는 삭제됐다.** 그 경로는 `plan.md` §B.1이 스스로 "최고 리스크 — 설계 미확정"으로 표시한 지점이었고, 2차·3차 감사가 그 표면에서만 MUST-FIX 3건을 냈다(N-MF-2 이관이 툼스톤을 우회 / N3-MF-2 이관 순서가 `plan.md`와 모순 / N3-MF-1의 파급 경로). 함께 폐기된 AC 3건: AC-011(이관 멱등) · AC-013(이관 후 제거) · AC-026(툼스톤 게이트).

**REQ-IMESTATE-004 — 읽기는 어떤 입력에도 예외를 던지지 않는다** *(Unwanted / If-Then)*

**If** 대상 파일이 없거나, 비어 있거나, JSON으로 파싱되지 않거나, 파싱되지만 기대한 모양이 아니면, **then** 시스템은 "기록 없음"으로 취급하고 `undefined`를 반환한다. 예외를 전파하지 않는다.

- 기존 `readAll`의 동작(`ime-session-store.ts:88-99`)을 기기별 경로에서 그대로 보존한다. 신규 능력이 아니라 **회귀 방지**다.
- 근거: 손상된 세션 기록 하나가 `text`·`reset`·`doctor` 전체를 실패시켜서는 안 된다.

**REQ-IMESTATE-005 — 세션 종료 후 잔재가 남지 않는다** *(Event-driven / When)*

**When** `clearOriginalIme(serial)`이 호출되면, 시스템은 그 시리얼의 레코드를 무효화하고 이후 조회가 `undefined`를 반환하게 한다.

- **"잔재 없음"의 조작적 정의**: 그 시리얼의 **레코드 파일(`<enc>.json`)이 존재하지 않고**, 툼스톤(`<enc>.cleared`)이 존재하며, 조회가 `undefined`를 반환하는 상태. 툼스톤은 잔재가 아니라 REQ-008이 요구하는 **음성 기록**이다.
- **연산 순서는 툼스톤 우선이다**(§A.3-⑦): ① `<enc>.cleared` 생성 → ② `<enc>.json` 제거. 반대 순서는 그 사이 창에서 조회를 3단계 구 파일 폴백으로 떨어뜨려 좀비를 되살린다.
- **구 파일 기록만 있는 시리얼의 `clear`는 툼스톤을 반드시 만든다.** 그 시리얼은 레코드 파일이 없지만 구 파일에 기록이 남아 있으므로, 툼스톤 없이는 이후 조회가 3단계 폴백으로 그 기록을 되살린다 — **이것이 REQ-008의 좀비 폐쇄 지점**이다. 0.4.0에서 구 파일이 영구히 남으므로 이 경우는 한시적 이관 창이 아니라 **영구 조건**이며, 툼스톤이 유일한 방어다.
- **레코드도 구 파일 기록도 없었다면** 조회 결과를 바꾸지 않는다(현행 no-op 의미 보존 — 툼스톤 생성은 멱등하다). 0.2.0은 이 단서를 "레코드가 애초에 없었다면"으로만 써서 무조건형으로 읽혔고, 그 독법이 위 불릿과 정면으로 충돌했다(2차 감사 N-MF-1) — 문자대로 구현하면 구 파일 기록이 남은 시리얼에서 툼스톤이 생성되지 않아 REQ-008이 무력화된다.

**REQ-IMESTATE-006 — 호출자는 수정되지 않는다** *(Ubiquitous)*

`ImeSessionStore`의 공개 메서드 3개 — `getOriginalIme(serial)` · `setOriginalIme(serial, originalIme)` · `clearOriginalIme(serial)` — 의 시그니처와 반환 계약은 불변이다. 직접 호출자 `AdbBackend`와 간접 호출자 `src/cli/commands/reset.ts` · `src/cli/commands/doctor.ts`(둘 다 `AdbBackend.getTrackedOriginalIme`/`clearTrackedOriginalIme` 경유)는 이 SPEC에서 수정되지 않는다.

- 이 요구는 §D의 제외 항목을 **검증 가능한 형태로 재진술**한 것이다 — 중복은 의도적이며, AC 4건이 여기에 걸린다.
- 테스트 주입 가능성도 보존한다: 경로와 I/O는 계속 생성자로 주입할 수 있어야 하며, 테스트가 실제 `~/.cache`를 건드리지 않는 현재 성질(`ime-session-store.ts:75-86`)을 유지한다.
- I/O 인터페이스에는 배타 생성·삭제·존재 확인 연산이 **추가**될 수 있다(가법 확장). 기존 `read`/`write`의 시그니처는 바뀌지 않는다.
- 공개 export 표면의 변경 범위는 §C.2에 열거한다.

### B.2 동시성 (REQ-IMESTATE-007 · 008 — 0.2.0 신설)

**REQ-IMESTATE-007 — 동일 시리얼의 부재식 기록은 원자적이다** *(Unwanted / If-Then)*

**If** 같은 시리얼에 대해 두 개 이상의 프로세스가 동시에 "기록이 없을 때만 기록한다"를 시도하면, **then** 정확히 하나만 성공하고 나머지는 **먼저 기록된 값을 보존한다**(덮어쓰지 않는다).

- 확정 기전(§A.3-⑤): 레코드 파일을 **배타 생성**한다. 이미 존재하면 생성이 `EEXIST`로 실패하고, **`EEXIST`에 한해** 오류가 아니라 "다른 프로세스가 이겼다"는 정상 결과로 처리한다.
- **그 밖의 실패는 삼키지 않는다 — 호출을 거부한다.** `ENOENT`(부모 디렉터리 부재) · `EACCES` · `ENOSPC` 등 비-`EEXIST` 실패는 `setOriginalIme`이 **거부(rejected promise)**로 종료해 호출자에게 전파한다. `EEXIST`는 거부하지 않고 정상 반환하며 기존 값을 보존한다. 이 **거부 / 정상반환의 이분**이 "구별되어 처리된다"의 관찰 가능한 정의다 — 0.3.0은 관찰 대상을 명시하지 않아 AC-027이 판정 불가였고, 이는 1차 감사가 잡은 공허 검사의 재발이었다(3차 감사 N3-MF-3).
- **이 실패 처리 규칙은 레코드와 툼스톤의 모든 생성·제거에 적용된다.** `clear`의 툼스톤 생성이 비-`EEXIST`로 실패하면 그 호출도 거부한다 — 조용히 성공하면 호출자는 무효화됐다고 믿지만 레코드가 남아 REQ-005의 조작적 정의가 성립하지 않는다. 예외는 하나다: 툼스톤 **제거**(§A.3-⑧)의 `ENOENT`는 "이미 없음"이므로 정상이다(멱등).
- REQ-004(읽기는 예외를 던지지 않는다)와 충돌하지 않는다 — 거부는 **쓰기 경로에만** 적용된다. 0.4.0에서 이관을 제외해 **조회가 쓰기를 유발하는 경로가 사라졌으므로**, 두 요구의 적용 범위는 완전히 분리된다(3차 감사 N3-SF-3이 지적한 미정의 교차가 기능 제거로 소멸했다). 0.2.0은 "실패는 오류가 아니라 정상 결과"를 종류 한정 없이 써서 `try { wx } catch { /* 남이 이겼다 */ }` 구현을 허용했고, 그러면 **기록이 전혀 남지 않은 채 정상 종료**해 `reset`이 복원할 대상을 잃는다 — 이 SPEC이 없애려는 결함과 사용자에게 보이는 증상이 동일한 새 경로다(2차 감사 N-MF-3).
- 이 요구가 닫는 것: `adb-backend.ts:454-457`의 check-then-act. 그 경합은 늦게 진입한 프로세스가 **이미 ADBKeyBoard로 바뀐 IME를 "원래 IME"로 기록**하게 만들고, 결과는 `reset`이 기기를 ADBKeyBoard로 "복원"하는 것 — 결함 ①과 동일한 증상이다.
- 잠금을 쓰지 않는다: 단일 시스템 콜의 원자성에만 의존하므로 대기·타임아웃·부생 상태가 없다(§A.3-② 무저촉).
- 호출자 `adb-backend.ts:454-457`은 수정하지 않는다(REQ-006) — 저장소가 그 의도를 원자적으로 이행한다.

**REQ-IMESTATE-008 — 무효화된 기록은 되살아나지 않는다** *(Ubiquitous)*

시스템은 무효화(`clear`)된 시리얼의 기록을 어떤 경로로도 되살리지 않는다. 조회는 다음 3단계를 **이 순서로** 따른다:

```
1. 레코드 파일 <enc>.json 존재    → 그 값을 반환
2. 없고 툼스톤 <enc>.cleared 존재 → undefined 반환, 구 파일 폴백 억제
3. 둘 다 없음                     → 구 파일 폴백 (REQ-003)
```

- 이 요구가 닫는 것: 0.1.0 §C.2가 허용된 한계로 두었던 **구 파일 좀비** — `clear`된 시리얼의 기록이 구 파일에 남아 있으면 이후 조회가 그것을 되살릴 수 있었다. 2단계가 그 부활을 차단한다. 0.4.0에서 구 파일이 영구히 남으므로 이 차단은 **영구적으로 유효해야** 한다 — 이관이 끝나면 해소되는 한시적 문제가 아니다.
- 툼스톤은 레코드 파일과 다른 경로이므로 REQ-007의 배타 생성과 간섭하지 않는다. 새 세션이 시작될 때 레코드 파일을 배타 생성하고 툼스톤은 멱등하게 제거하며, 그 **순서는 §A.3-⑧이 확정한다**(레코드 먼저). (경로가 같았다면 툼스톤 존재 시 배타 생성이 `EEXIST`로 실패하고 REQ-007의 "기존 보존"이 **툼스톤을 보존**해 조회가 영구히 `undefined`가 된다 — 기기 복원 불가. 경로 분리가 그 위험을 근본적으로 없앤다.)
- **1단계가 2단계보다 앞서므로 레코드를 만드는 경로는 툼스톤을 이긴다.** 0.4.0에서 레코드를 만드는 경로는 `setOriginalIme`(새 세션 시작) **하나뿐**이고, 새 세션이 무효화 상태를 대체하는 것은 의도된 동작이므로 **게이트가 필요하지 않다** — §A.3-⑧이 그 경로의 순서만 확정한다. 0.3.0은 이관이라는 **두 번째** 레코드 생성 경로를 가졌고 그 경로에는 무효화를 대체할 권한이 없었으므로 게이트가 필요했다(2차 감사 N-MF-2). 이관을 제외하면서 게이트도 함께 삭제됐다 — 지켜야 할 대상이 사라졌기 때문이다.
- **`clear` 연산 순서는 툼스톤 우선이다**(REQ-005 / §A.3-⑦). 반대 순서의 중간 창은 3단계 폴백을 열어 이 요구를 스스로 위반한다.
- **잠금이 필요하지 않다**: 한 시리얼의 툼스톤을 쓰는 주체는 그 시리얼의 프로세스뿐이며, 생성은 멱등하다.

---

## §C. 제약 (Constraints)

### C.1 출처 (SOURCES) — 무엇을 관측했고 무엇을 못 했나

**관측한 것** (2026-07-29, 코드 읽기 + 명령 실행 + 감사):

| # | 관측 | 근거 |
|---|---|---|
| ① | 결함 ①이 코드 주석에 이미 기록되어 있다 | `ime-session-store.ts:22-27` `@MX:NOTE` |
| ② | 저장 위치는 모든 기기가 공유하는 단일 파일이다 | 같은 파일 `:13-20`, `:49-54` |
| ③ | 매 CLI 호출은 별개 OS 프로세스다 | 같은 파일 `:5-11` |
| ④ | 명령 표면은 12개다 | `src/cli/router.ts:34-45` |
| ⑤ | `devices`를 제외한 11개 명령이 `--device`로 기기를 지정한다 | `resolveTargetDevice` 사용 파일 목록 |
| ⑥ | `dump`는 **동일 시리얼 동시 호출까지** 랜덤 접미사로 방어한다 | `adb-backend.ts:49-59` |
| ⑦ | `screenshot`은 기기측 임시 파일을 쓰지 않는다(동시성 언급 없음 — 스트리밍의 부수 결과) | `screenshot.ts:4-8` |
| ⑧ | 검증 3종이 통과 상태다 — 702 tests / 32 files, typecheck·build exit 0 | `pnpm test` · `pnpm typecheck` · `pnpm build` 실행 |
| ⑨ | 손실 있는 변환이 두 시리얼을 실제로 같은 값으로 붕괴시킨다 | 실행 확인: `192.168.1.5:5555` · `192_168_1_5_5555` → 둘 다 `192_168_1_5_5555` |
| ⑩ | 이 패키지는 아직 배포되지 않았다 — 외부 소비자가 없다 | `.moai/project/product.md:105` |
| ⑪ | **결정적 인터리빙 유실이 재현됐다** | 현행 `ImeSessionStore` + 주입 IO에 읽기 배리어를 걸어 실행 — 최종 디스크 `{SERIAL-A}`만 남고 `getOriginalIme("SERIAL-B") === undefined` |
| ⑫ | **결함 ②(동일 시리얼 check-then-act)가 코드에 실재한다** — 코드 **독해**로 확인한 구조적 사실이며, 유실 재현은 별개다(㉑) | `adb-backend.ts:454-457` + 그 목적을 밝힌 `:446-452` 주석 |
| ⑬ | **무방어 공유 자원이 하나가 아니라 둘이다** | `src/webview/calibration.ts` `CalibrationStore` — 같은 캐시 디렉터리, 단일 공유 파일, 동일한 비원자 read-modify-write, 주석에 "mirroring `ImeSessionStore`" |
| ⑭ | 소문자 hex가 대소문자만 다른 시리얼을 분리한다 | 실행 확인: `ABC123`→`414243313233`, `abc123`→`616263313233` |
| ⑮ | `ImeSessionStore` 관련 export는 5종이다 | `src/index.ts:30-34` |
| ⑯ | `reset.ts`·`doctor.ts`는 `ImeSessionStore`를 직접 참조하지 않는다(간접 호출자) | 직접 참조는 `calibration.ts`·`index.ts`·`ime-session-store.ts`·`adb-backend.ts` 4파일 |

**관측하지 못한 것**:

| # | 미관측 | 영향 |
|---|---|---|
| ⑰ | **실제 프로세스 타이밍 유실** — 별개 OS 프로세스 2개에서 자연 발생하는 유실은 관측하지 않았다 | ⑪은 배리어로 **강제한** 인터리빙이다. 구조적 결함의 증명으로는 충분하지만, 실사용 발생 빈도는 미측정. M5가 다룬다 |
| ⑱ | 실기기 2대 동시 연결 상태의 end-to-end | 결함은 **호스트 측 파일 I/O**이므로 기기 없이 두 프로세스만으로 실측 가능하다(§C.3). AC-020만 실기기를 요구 |
| ⑲ | 대소문자 무구분 볼륨 특성 자체 | ⑭는 인코더가 대소문자를 붕괴시키지 않음만 증명한다. 볼륨이 실제로 무구분인지는 AC-005의 프로브가 확인한다 |
| ⑳ | `CalibrationStore` 유실의 실제 재현 | ⑬은 **구조적 동일성**까지만 주장한다. §C.4로 이월 |
| ㉑ | **결함 ②의 강제 인터리빙 재현** — 이 SPEC은 결함 ②의 유실을 관측하지 않았다(⑫는 코드 독해까지만) | M1이 수행한다. 결함 ①은 ⑪에서 관측됐으나 그 관측이 결함 ②로 자동 전이되지는 않는다 — 두 결함은 다른 코드 경로(저장소 내부 RMW vs 호출자 check-then-act)다 |
| ㉒ | **0.4.0 개정 자체가 감사받지 않았다** — plan-auditor 반복이 3/3 소진된 뒤 수행한 편집이다 | 대체 검증 2종을 실행했다: ① 삭제 대상(§A.3-⑧ 이관 규칙 · AC-011·013·026 · `plan.md` §B.1)을 가리키는 **참조 전수 grep** ② 기존 702개 테스트·typecheck·build(문서 개정이므로 코드 무영향 확인). **대체 검증은 감사와 동등하지 않다** — 참조 무결성과 회귀 부재만 보이며, 개정된 요구사항 자체의 정합성은 검증하지 않는다. M1~M5 착수 시 0.4.0이 손댄 범위(REQ-003 · REQ-007 · §A.3-⑧ · AC-027·028)를 특히 의심할 것 |

**재현 하네스에 관한 관측** (0.2.0 추가 — AC-002·003의 근거): 순진한 하네스는 재현에 **실패한다.** `setOriginalIme`이 자기 내부에서 다시 읽으므로, 겹쳐야 하는 지점은 메서드 바깥이 아니라 **안쪽**이다. 순차 await로 짠 첫 시도는 두 기록이 모두 살아남았고(거짓 음성), 읽기 배리어를 넣은 뒤에야 재현됐다. 또한 가짜 디스크를 단일 변수로 두면 수정 후에도 경로 차이를 무시해 **거짓 실패**가 난다 — 가짜 IO는 반드시 **경로별로 키를 나눈 저장소**여야 한다.

### C.2 알려진 한계

- **공개 export 표면 변경 (5종 전부)** — `src/index.ts:30-34`가 export하는 5개 심볼이 모두 영향을 받으며, 그중 4개는 0.2.0에서 **새로 자백한 것**이다(0.1.0은 생성자 arg1만 인정했다):

  | export 심볼 | 변경 | 처분 |
  |---|---|---|
  | `ImeSessionStore` 생성자 arg1 | 파일 경로 → 디렉터리 경로 | 의미 변경 (M2) |
  | `resolveImeSessionStorePath()` | 반환값이 파일(`ime-sessions.json`) → 구 파일 경로 전용으로 의미 축소 | 유지 + 용도 재정의 (**M3** 읽기 전용 폴백용) |
  | `ImeSessionMap` | 단일 파일 전체 맵 형태 — 기기별 파일에서는 구 파일 파싱 전용으로만 의미가 남는다 | 유지 + 용도 축소 (**M3**) |
  | `ImeSessionRecord` | REQ-002가 원본 시리얼 필드를 요구하므로 형태 변경 | 형태 확장 (M2) |
  | `ImeSessionStoreIO` | 배타 생성·삭제 연산 가법 추가 | REQ-006이 명시 허용 |

  이 패키지는 npm에 배포되지 않았고(§C.1-⑩) 명시적 경로를 넘기는 호출자는 테스트뿐이므로 실질 파급은 저장소 내부와 테스트에 한정된다. 다만 미배포가 **열거를 대체하지는 않는다.**
- **iOS는 이 결함과 무관하다** — `idb` 텍스트 입력은 IME 상태를 남기지 않으며(`reset.ts:21-22`), 복원할 세션 개념 자체가 없다. 따라서 이 결함은 **Android 기기 2대** 또는 **Android 1대에 대한 동시 호출**에서만 발생하고, Android 1대 + iOS 시뮬레이터 1대 조합에서는 발생하지 않는다.
- **비-ASCII 입력에만 해당한다** — ASCII 텍스트는 IME 전환 없이 입력되므로 세션 기록이 생기지 않는다.
- **툼스톤은 무기한 누적될 수 있다** — REQ-008의 툼스톤에 만료·정리 규칙을 두지 않는다. 시리얼 수만큼만 생기고 크기가 상수이므로 실무상 누적 비용이 무시 가능하며, 정리 규칙을 도입하면 "언제 지워도 안전한가"라는 새 판단이 필요해진다(그 판단이 틀리면 좀비가 되살아난다). 누적을 택한다.
- **구 단일 파일은 영구히 남는다 (0.4.0 신설 — 범위 축소의 대가)** — REQ-003이 이관·폐기를 제외했으므로 `<cache-dir>/ime-sessions.json`은 지워지지 않는다. 항목이 조회에서 빠지는 것은 삭제가 아니라 **가려짐**(레코드 1단계 / 툼스톤 2단계)으로만 일어난다. 비용은 캐시 디렉터리의 작은 JSON 1개가 상주하는 것이고, 얻는 것은 **유실 경로의 원천 소멸**(아무것도 이동·삭제하지 않으므로 이관 중 크래시·경합으로 기록을 잃을 방법이 없다)이다. 바로 위 툼스톤 누적과 **같은 종류의 거래**임을 밝혀 둔다 — 정리하지 않는 편이 정리 판단을 틀리는 것보다 안전하다.
- **구 파일에 남은 기록은 무기한 폴백 대상이다** — 파생 결과로, 아주 오래전 세션의 기록이 그 시리얼에 레코드도 툼스톤도 없는 상태에서 조회되면 반환될 수 있다. 이는 REQ-003이 의도한 동작이지만(복원 기록을 잃지 않는다), 낡은 IME 값이 반환될 가능성은 남는다. 0.3.0의 이관 설계도 이관 시점까지는 동일했고, 이관은 그 가능성을 **없애지 못하고 종료 시점만 정했다** — 그 종료 시점을 얻는 대가가 MUST-FIX 3건이었다.

> 0.1.0 §C.2의 **"이관 창 좀비"** 항목은 삭제됐다 — REQ-008(툼스톤)이 그 부활 경로를 닫는다. 함께 있던 **"닫으려면 잠금이 필요하다"**는 진술은 미검증이었고 틀렸으므로 철회한다(§A.3-⑥).
>
> 0.4.0에서 "이관 창"이라는 개념 자체가 사라졌다 — 이관이 없으므로 창도 없고, 구 파일 좀비는 한시적 창이 아니라 **영구 조건**이 됐다. 툼스톤이 그 유일한 방어이며(§A.3-⑥), 그래서 툼스톤의 역할이 0.3.0보다 무거워졌다.

### C.3 검증이 기기를 요구하지 않는다 (설계상 이점)

이 결함은 기기 동작이 아니라 **호스트의 파일 I/O**에서 발생한다. 따라서 서로 다른 시리얼 문자열 2개(결함 ①) 또는 같은 시리얼 문자열(결함 ②)로 두 프로세스를 동시에 돌리는 것만으로 **실제 재현과 실제 수정 확인이 모두 가능하다** — 실기기 2대가 필요하지 않다.

이는 "mock은 argv 너머를 못 본다"는 기존 교훈과 충돌하지 않는다. 그 교훈은 외부 CLI(`adb`/`idb`) 호출 결과를 mock으로 판정할 수 없다는 것인데, 여기서 판정 대상은 외부 CLI가 아니라 **우리가 직접 쓰는 파일**이다. 두 프로세스 실측이 곧 실제 판정이 된다.

단, §C.1의 재현 하네스 관측이 경고하듯 **하네스를 순진하게 짜면 거짓 음성·거짓 실패가 모두 발생한다.** AC-002·003이 하네스 요건을 명시한다.

### C.4 다른 SPEC으로 넘기는 후속 (cross-SPEC follow-up)

- **웹 캘리브레이션 캐시 (`src/webview/calibration.ts`, REQ-WEB-ACT-005)** — `CalibrationStore`가 **같은 결함군**이다: 같은 캐시 디렉터리(`resolveApkCacheDir()`), 단일 공유 파일 `web-calibration.json`, `readAll()` → `all[serial] = record` → 전체 write의 비원자 read-modify-write. 주석 스스로 "mirroring `ImeSessionStore`"라고 적는다. **이월 근거는 피해 등급 차이다** — IME 기록 유실은 사람이 기기를 손으로 되돌려야 하는 복원 불가로 직결되지만, 캘리브레이션 유실 비용은 재측정 1회다. 이월은 정당하나 **미공개는 정당하지 않으므로** 여기에 명시한다. 유실의 실제 재현은 하지 않았다(§C.1-⑳).
- **ADBKeyBoard APK 캐시 디렉터리의 동시 접근** — IME 세션 저장소와 같은 캐시 디렉터리를 공유하지만 다운로드/설치 경로는 별개 자원이며 이번 점검에서 결함을 확인하지 않았다. 별도 점검 대상.
- **탐색 루프 · 재시도 전략 · 화면 판단** — `interview.md:39`의 OUT 경계. 이번 점검에서 그 경계를 바꿀 근거는 나오지 않았다. 에이전트 책임으로 유지.

---

## §D. 범위에서 제외 (Exclusions)

### Out of Scope — 구현 세부(HOW)

구현 방식은 원칙적으로 범위 밖이다. **단 두 가지는 예외로 SPEC 층위에서 확정한다**: ① 시리얼→파일명 인코딩(REQ-002) — 온디스크 형식은 이후 판독자에 대한 **호환 계약**이고, §A.3-④가 특정 대안을 SPEC 층위에서 기각했으므로 대체 규칙을 같은 층위에 두는 것이 대칭적이다. ② 배타 생성과 툼스톤(REQ-007·008) — 감사가 지적했듯 **정확성에 직결되는 기전을 구현 재량으로 미루면** 요구사항이 금지한 선택지가 되살아난다.

### Out of Scope — 파일 락

§A.3-② 기각. REQ-007·008이 잠금 없이 남은 두 경합을 닫으므로 이 기각은 사각지대를 남기지 않는다.

### Out of Scope — 원자적 쓰기를 요구사항으로 삼기

§A.3-③에 따라 temp+rename은 결함 ①의 해결책이 아니다. 부분 쓰기 견고성 목적의 채택은 구현 재량이지만 요구사항이 아니다.

### Out of Scope — 구 파일 항목별 삭제

공유 파일 read-modify-write이므로 **REQ-001 위반**이다. 구 파일에는 쓰지 않는다(REQ-003).

### Out of Scope — 구 파일 이관·폐기 (0.4.0 신설)

구 단일 파일의 기록을 신규 레코드 파일로 옮기는 **이관**, 그리고 옮긴 뒤 구 파일을 없애는 **폐기**(통째 rename · 표식 파일 · 삭제) 전부가 범위 밖이다. 구 파일은 읽기 전용 폴백으로 영구히 남는다(REQ-003).

제외 근거는 두 가지다. ① **필요하지 않다** — 항목이 조회에서 빠지는 것은 이관이 아니라 가려짐(레코드 1단계 / 툼스톤 2단계)으로 이미 달성된다. ② **위험이 크다** — 3라운드의 감사에서 이 경로만 MUST-FIX 3건을 냈고(이관이 툼스톤을 우회 / 이관 순서가 `plan.md`와 모순 / 그 파급), `plan.md` §B.1이 스스로 "최고 리스크 — 설계 미확정"으로 표시한 유일한 지점이었다. 아무것도 이동·삭제하지 않는 설계가 **엄격히 더 안전하다** — 이관 중 크래시나 경합으로 기록을 잃을 방법이 존재하지 않는다.

대가(구 파일 영구 잔존)는 §C.2에 한계로 기록했다. 이 제외를 되돌리려면 별도 SPEC이 필요하며, 그 SPEC은 이관 **순서**를 SPEC 층위에서 먼저 확정해야 한다 — 순서를 구현 재량에 두는 것이 3차 감사 N3-MF-2의 직접 원인이었다.

### Out of Scope — 툼스톤 만료·정리 규칙

§C.2에 누적을 택한 근거를 기록.

### Out of Scope — 호출자 수정

`AdbBackend`(직접) · `reset.ts` · `doctor.ts`(간접)를 수정하지 않는다. REQ-006이 검증 가능한 형태로 재진술한다.

### Out of Scope — `DeviceBackend` 인터페이스 변경

10개 메서드 표면 불변(PRESERVE).

### Out of Scope — iOS 세션 상태 도입

§C.2에 따라 복원할 상태가 존재하지 않는다.

### Out of Scope — 웹 캘리브레이션 캐시 · APK 캐시 동시성

둘 다 §C.4로 이월.

### Out of Scope — 탐색 루프 / 멀티 기기 시나리오 오케스트레이션

`interview.md:39` OUT 경계. 로드맵의 "SPEC-04" 자리표시자는 이 SPEC과 §C.4로 해소되며, 별도 기능 SPEC으로 부활하지 않는다.
