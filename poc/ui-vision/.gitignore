# 가상환경 — 로컬에서 uv로 재생성
.venv/

# 다운로드한 모델 가중치 — 용량이 크고 Hugging Face에서 다시 받을 수 있음
models/*
!models/.gitkeep

# 실기기 캡처 결과물 — 실제 화면 스크린샷은 개인정보(연락처, 메시지 등)를
# 담을 수 있어 커밋하지 않는다 (auto-memory: "검증 캡처는 식별정보를 흘린다").
output/*
!output/.gitkeep

__pycache__/
*.pyc

# API 키 등 비밀값 — 절대 커밋하지 않는다
.env
.env.*
