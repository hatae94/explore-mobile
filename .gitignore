# ============================================
# MoAI-ADK Package Distribution .gitignore
# ============================================
# Version: 0.33.0
# Updated: 2026-01-10
# Purpose: Essential patterns for user projects
# ============================================

# ===========================================
# Go Build Artifacts
# ===========================================
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
cover.out
cover.html
cpu.prof
mem.prof

# ===========================================
# Build Artifacts & Distribution
# ===========================================
build/
dist/
*.egg-info/
*.egg

# ===========================================
# Python Cache & Bytecode
# ===========================================
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
.mypy_cache/
.ruff_cache/
*.db
*.sqlite
*.sqlite3
*.rdb
*.dbm

# ===========================================
# Virtual Environments
# ===========================================
venv/
.venv/
env/
.env/
.uv_cache/
.uv-cache/

# ===========================================
# Test Coverage
# ===========================================
.coverage
coverage.json
coverage/
htmlcov/

# ===========================================
# IDE & Editors
# ===========================================
.vscode/
.idea/
*.swp
*.swo
*.sw?
*~
.cursor/
*.sublime-project
*.sublime-workspace
*.njsproj
*.sln
*.suo
.#*

# ===========================================
# OS Files
# ===========================================
# Mac
.DS_Store
.AppleDouble
.LSOverride
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns

# Windows
[Dd]esktop.ini
Thumbs.db
ehthumbs.db

# Linux
.directory

# ===========================================
# Logs & Temporary Files
# ===========================================
*.log
logs/*
!logs/.gitkeep
*.tmp
*.temp
*.bak
*.backup

# ===========================================
# Backups
# ===========================================
.moai-backups/
.moai/backups/
*.backup/
*-backup/
*_backup_*/

# ===========================================
# Secrets and Credentials (CRITICAL)
# ===========================================
# Environment files
.env
.env.*
!.env.example

# Certificate files
*.pem
*.p12
*.pfx
*.jks

# Private keys (specific patterns to avoid false positives)
# FIXED: Issue #237 - Replaced overly broad *key* and *secret* patterns
*-key.*
*_key.*
*.key.*
*-secret-key.*
*-api-key.*
api-key-*
secret-key-*
private-key*
*-secret.*
*_secret.*
*.secret.*
secret-*.*

# Credential patterns in environment files
*_secret=*
*_token=*

# Cloud provider credentials
.aws/config
.aws/credentials

# MINK agent credentials (plaintext API keys, per AC-CR-026).
# Ignored anywhere in the tree so per-project auth dirs are never committed.
**/.mink/auth/

# ===========================================
# Claude Code Configuration
# ===========================================
.claude/settings.local.json
.claude/settings.local.*.json
.claude/.env.local*
.claude/local/
.claude/plans/
.claude/agent-memory/
.claude/worktrees/

# Skill-specific credentials
.claude/skills/*/.env*
.claude/skills/*/secrets/
.claude/skills/*/*/*secret*
.claude/skills/*/*/*key*
.claude/skills/*/*/*token*
# Re-include legitimate skill module files containing "token" in their name
!.claude/skills/*/modules/*token*.md

# ===========================================
# Local-Only Files (never sync via git pull)
# ===========================================
.moai/status_line.sh
.moai/memory/
.moai/worktrees/
.moai/branches/

# ===========================================
# Update Noise-Suppression Ledgers (per-machine, never tracked)
# Per-machine state + cache ledgers used to suppress redundant update noise.
# ===========================================
.moai/state/
.moai/cache/
# 중첩 경로도 막는다. 위 두 줄은 저장소 루트에만 적용되는데, 런타임이 cwd를
# 잘못 잡으면 `.moai/specs/<SPEC>/.moai/state/`처럼 하위에 상태 파일을 흘린다
# (실제로 흘린 것을 추적할 뻔했다).
**/.moai/state/
**/.moai/cache/

# ===========================================
# Plan Audit Reports (local artifacts)
# Plan-audit verdict reports are local artifacts; only .gitkeep is tracked.
# ===========================================
.moai/reports/plan-audit/*.md
!.moai/reports/plan-audit/.gitkeep

# ===========================================
# Evolution Telemetry (local-only usage data)
# Learnings and new-skills are git-tracked for team sharing.
# ===========================================
.moai/evolution/telemetry/*.jsonl
!.moai/evolution/telemetry/.gitkeep

# ===========================================
# Node.js (for docs/Nextra)
# ===========================================
node_modules/
*.tsbuildinfo
.tscache/
.next/
out/

# ===========================================
# Build Tools & Frameworks
# ===========================================
.turbo/
.nuxt/
dist-ssr/

# ===========================================
# Deployment & CI/CD
# ===========================================
.vercel/
.netlify/
.firebase/
# ===========================================
# 실기기 검증 캡처 (SPEC-IMAGE-001 M5)
# ===========================================
# 판정 결과는 progress.md §E.2와 image-verification/README.md에 남긴다.
# 이미지 자체는 로컬 증거로만 보존한다 — 기기 식별정보·개인 데이터가
# 찍히기 쉽고(IMEI, 실명, 메일 본문) 저장소를 비대하게 만든다.
.moai/reports/*-verification/*.png
.moai/reports/*-verification/*.jpeg
.moai/reports/*-verification/*.jpg
.moai/reports/*-verification/*.geometry.json

# ===========================================
# 런타임이 쓰는 파일 (사람이 고치지 않는다)
# ===========================================
# 훅·하네스가 세션마다 append하는 기록이라 커밋하면 매 세션 diff가 생긴다.
# 디렉터리 자체는 .gitkeep으로 남긴다.
.moai/logs/*
!.moai/logs/.gitkeep
.moai/harness/usage-log.jsonl
.moai/lessons-inbox.jsonl

# 훅 호출 통계 요약 — 세션마다 파일이 하나씩 늘어나는 로컬 산출물.
.moai/reports/session-*.md

# 다이어그램 시각 점검 산출물 — 본 다이어그램(explore-mobile-architecture.*)만
# 추적하고, 해상도·테마별 점검 이미지는 다시 만들 수 있으므로 제외한다.
.moai/reports/diagrams/*.visual-check.*

# 에이전트 메모리 — 위 .claude/agent-memory/ 규칙은 저장소 루트에만 걸린다.
# 훅이 cwd를 잘못 잡으면 .moai/specs/<SPEC>/.claude/agent-memory/처럼 하위에
# 흘리므로(실제로 흘렸다) 중첩 경로도 함께 막는다.
**/.claude/agent-memory/

# Claude Code 로컬 설정의 잠금 파일 — 기기별 파일이다.
.claude/settings.local.json.lock
